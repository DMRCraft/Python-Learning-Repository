# ARGUMENTS

## When would i use keyword arguments?

Use for some cases like:

- Python libraries normally have functions with lots of optional settings. Using keyword arguments means you dont need to memorise the position of each
- They are also useful if a function you make has alot of parameters, and using keyword arguments is more efficient than memorising the position of each
