# Membership Operators

## == vs in

Use == when you want to check if two values are equal <br>
Use in when you want to see "does this contain this"

```python
numbers = [1, 2, 3, 4, 5]

5 in numbers # True
5 == numbers # False
```
