# Quick Tips:

- With `__main__`, if you want a closing message / code, do it like this:

```python
def main():
    while True:
        # ...
        break
    # runs right before the main function ends
    print("Closing message")

```

- Passing
